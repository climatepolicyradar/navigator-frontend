# ESC environment: frontend/cpr-review
# Configuration for the cpr-review stack and its PR review stacks.
# Imports AWS credentials from the shared aws-creds-staging environment.
imports:
  - frontend/aws-creds-staging

values:
  pulumiConfig:
    docker_tag: ${docker_tag}
  docker_tag: latest
