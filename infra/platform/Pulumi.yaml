name: frontend-platform
description: Manages OIDC identity provider, IAM deployment roles, ESC environments, and deployment settings for the frontend project
runtime:
  name: python
  options:
    toolchain: uv
    virtualenv: .venv
