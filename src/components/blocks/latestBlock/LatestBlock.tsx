import { TLatestFamily } from "@/types";

export interface IProps {
  latestFamilies: TLatestFamily[];
}

export const LatestBlock = ({ latestFamilies }: IProps) => {
  return <div className=""></div>;
};
